def props = new Properties()
file("$rootDir/base/app_version.properties").withInputStream { props.load(it) }

ext {
    config = [
            'versionCode': props.getProperty("appVersionCode").toInteger(),
            'versionName': props.getProperty("appVersionName"),
            'minSDK': 19,
            'currentSDK': 28
    ]
}